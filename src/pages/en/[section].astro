---
import InteriorPage from "../../components/InteriorPage.astro";
import Documentation from "../../components/Documentation.astro";
import SiteLayout from "../../layouts/SiteLayout.astro";
import { pages, type Section } from "../../data/pages";
import { docs } from "../../data/docs";
import { artifacts } from "../../data/releases";
import { highlightCode } from "../../lib/highlight";
import { formatInlineCode } from "../../lib/inline-code";
import { site } from "../../data/site";

export function getStaticPaths() {
  return (["learn", "examples", "architecture", "journal", "download"] as const).map((section) => ({ params: { section }, props: { section } }));
}

const { section } = Astro.props as { section: Section };
const page = pages.en[section];
const documentation = docs.en[section];
const learnSections = docs.en.learn!.sections;
const helloCode = `fonction principal() {
  afficher("Bonjour, monde!")
}`;
const architectureCode = `source
  -> tokens
  -> AST
  -> runtime values
  -> tree-walk or VM
  -> diagnostics`;
const journalCode = `current release
language guide
command line
source builds
release notes`;
const buildCode = `git clone ${site.repository}
cd lumiere
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON
cmake --build build --parallel 2
ctest --test-dir build --output-on-failure
sudo cmake --install build
lumiere --version`;
---
<SiteLayout locale="en" title={`${page.eyebrow} — Lumière`} description={page.intro}>
  <InteriorPage locale="en" section={section} {...page}>
    {section === "learn" && (
      <>
        <div class="lead">
          <div>
            <h2>{page.heading}</h2>
            <p class="lede">{page.body}</p>
          </div>
          <div class="panel code-panel">
            <div class="panel-head"><span>bonjour.lum</span><span>first program</span></div>
            <pre><code set:html={highlightCode(helloCode)} /></pre>
          </div>
        </div>
        <nav class="doc-index" aria-label="Learning documentation index">
          <div class="doc-index-head">
            <span>Documentation index</span>
            <a href="/en/learn/tutorial/#setup">Open tutorial</a>
          </div>
          <div class="doc-index-list">
            {learnSections.map((tutorialSection, index) => (
              <a href={`/en/learn/tutorial/#${tutorialSection.id}`}>
                <span>{String(index + 1).padStart(2, "0")}</span>
                <strong>{tutorialSection.title}</strong>
                <p set:html={formatInlineCode(tutorialSection.body[0] ?? "")} />
              </a>
            ))}
          </div>
        </nav>
      </>
    )}

    {section === "architecture" && (
      <>
        <div class="lead">
          <div>
            <h2>{page.heading}</h2>
            <p class="lede">{page.body}</p>
          </div>
          <div class="panel code-panel">
            <div class="panel-head"><span>release {site.version}</span><span>architecture map</span></div>
            <pre><code set:html={highlightCode(architectureCode)} /></pre>
          </div>
        </div>
        <div class="index">
          <a href="#french-source"><span>01</span><strong>French source</strong><p>How Lumière accepts accented identifiers, French keywords, Unicode symbols, and ASCII aliases.</p></a>
          <a href="#execution-pipeline"><span>02</span><strong>Execution pipeline</strong><p>How source moves through tokenization, parsing, diagnostics, tree-walk execution, and VM lowering.</p></a>
          <a href="#luminet-module"><span>03</span><strong>LumiNet</strong><p>How networking is built from capability objects, native state, sockets, HTTP routes, and Canal callbacks.</p></a>
        </div>
      </>
    )}

    {section === "journal" && (
      <>
        <div class="lead">
          <div>
            <h2>{page.heading}</h2>
            <p class="lede">{page.body}</p>
          </div>
          <div class="panel code-panel">
            <div class="panel-head"><span>journal</span><span>project notes</span></div>
            <pre><code set:html={highlightCode(journalCode)} /></pre>
          </div>
        </div>
        <div class="journal-list">
          <div class="empty-state">
            <span>No published articles yet</span>
            <strong>The journal will appear here once the first article is written.</strong>
            <p>Until then, use the Learn, Architecture, and Download pages for the current public documentation.</p>
          </div>
        </div>
      </>
    )}

    {section === "download" && (
      <>
        <div class="lead">
          <div>
            <h2>{page.heading}</h2>
            <p class="lede">{page.body}</p>
          </div>
          <div class="panel code-panel">
            <div class="panel-head"><span>build from source</span><span>build, test, install</span></div>
            <pre><code set:html={highlightCode(buildCode)} /></pre>
          </div>
        </div>
        <div class="release-table">
          {artifacts.map((artifact) => (
            <a href={artifact.href}>
              <span>{artifact.format}</span>
              <strong>{artifact.label}</strong>
              <small>{artifact.detail}</small>
              <i>Download</i>
            </a>
          ))}
          <a href={site.repository}>
            <span>source</span>
            <strong>Build with CMake</strong>
            <small>Use this path when no release artifact matches your CPU or operating system.</small>
            <i>GitHub</i>
          </a>
        </div>
      </>
    )}

    {documentation && section !== "learn" && <Documentation content={documentation} locale="en" />}
  </InteriorPage>
</SiteLayout>
